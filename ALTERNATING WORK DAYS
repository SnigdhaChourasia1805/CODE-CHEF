#include <iostream>
using namespace std;

int main() {
	int t;
	cin>>t;
	for(int i=0;i<t;i++){
	    int a,b,p,q;
	    cin>>a>>b>>p>>q;
	    if((p%a==0 && q%b==0) && (abs(p/a-q/b)<=1)){
	        cout<<"YES"<<endl;
	    }
	    else{
	        cout<<"No"<<endl;
	    }
	}
	return 0;
}
