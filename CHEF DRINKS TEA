#include <iostream>
using namespace std;

int main(void) {
	int t;
	cin >> t;
	while(t--)
	{
	    int x,y,z,ans;
	    cin >> x >> y >> z;
	    if(x%y==0)
	    ans=(z*x)/y;
	    else
	    ans=((x/y)+1)*z;
	    cout << ans << endl;
	}
	return 0;
}
