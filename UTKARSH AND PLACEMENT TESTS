#include <iostream>
using namespace std;

int main(void) {
    int T;
    cin >> T;

    for(int i=0; i<T; i++)
    {
        char a, b, c;
        cin >> a >> b >> c;

        char x, y;
        cin >> x >> y;

        if(x == a || y == a) cout << a;
        else if(x == b || y == b) cout << b;
        cout << endl;
    }

return 0;
}
